// == Import
import PropTypes from 'prop-types';
import './styles.scss';

// == Composant
function Counter() {
  return (
    <p className="counter">3&nbsp;tâche(s) en cours</p>
  );
}

Counter.propTypes = {
  
};

// == Export
export default Counter;
